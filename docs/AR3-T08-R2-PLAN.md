# T08-R2 — Historical-prefix replay and producer/commitment correlation

## Authorization and boundary

The user requested: “do next Next: historical-prefix replay and producer/commitment correlation.” Main assigns a new sequential Astra implementation stage; R1C is completed and must not be restarted. T01–07 remain accepted (7/10), full T08 is not accepted, T09/T10 are unstarted. The current run ID/status belongs in [the live task ledger](AR-03-AGENT-TASKS.md) and [findings](AR3-T08-FINDINGS.md).

- Starting `src/actor-model.js` SHA: `89974e46c09586ec8879ec79ca3aa19ea3b8cccf20db56b0d05f38809f934e4e`. Main independently reconfirmed canonical root `/Users/adam2/projects/pair/pi-fabric-pair` and this hash before assignment.
- Same approved Astra model (`openai-codex/gpt-6-astra`), one source writer, **read/grep/find/ls/edit only**. Verify identity first by reading absolute `package.json` (`pi-fabric-pair@0.1.0`); no shell identity command for the child.
- Exclusive editing: existing `src/actor-model.js`, focused replacements, not a whole-file rewrite. R2 authorizes concrete **model-local** event/producer/operation/view additions needed below, not changes to frozen leaf schemas or public export signatures. Document exact added shapes/mappings in the handoff.
- Main owns shared/common/facade/docs and all permitted static checks after the writer stops. All six contracts plus `coordination.js`/`transitions.js` stay frozen. An actual outside-file need requires an exact symbol/arguments/return/validation/budget dependency report before edits; neither this plan nor graph proximity authorizes external edits.
- No concurrent writer/reviewer, delegation, bash/write, alternate execution/provider workaround, tests, probes, fixtures, generated scripts, project ESM loads, installation, live/native workers, runtime activation, active format/default/schema/example/registration changes. Do not use/read/modify/delete `/tmp/tscheck.mjs`; preserve unrelated dirty work including `%1`.
- Read [testing policy](TESTING.md), [D1–D6](AR3-01-DECISIONS.md), [T06 exact types](AR3-T06-HANDOFF.md), [T07 obligations](AR3-T07-HANDOFF.md), and relevant source before editing. The generic historical DeepSeek/shell-writer wording is superseded by this explicit assignment.

## Witnessed starting execution path

At the starting hash, `runFold` (562–577) applies all actor events, then validates each complete kernel subsequence. `applyActorEvent` (604–619) only checks an implementation event's owner/fence and queues it; a workflow can therefore appear later. Actor plan/attempt/report/decision branches (639–783) maintain facts without exact committed-kernel correlation. Activation issuance (648–665) retains IDs/hashes rather than full producer/dispatch records; settlement (666–673) drops phase/time. The reducer's separate kernel shortcut (1125–1134) recognizes reject/noop but discards hold. These paths must share historical rules rather than form alternate admission routes.

The accepted kernel already validates its own journal prefixes. Its reducer calls full model validation and returns `apply/noop/hold/reject`; a held result can contain retained journal facts. T08 must correlate actor history at those prefixes, not rewrite kernel semantics or claim that repeated public validation is an incremental/shared-budget API.

## Acceptance ledger — R2 only

| ID | Required source behavior and verification |
|---|---|
| R2-1 Historical prefixes | Establish the workflow/kernel genesis before any implementation event. Advance the workflow's accepted kernel projection at each root prefix, including archived-root folds. Later workflow/activation/settlement/receipt facts cannot repair earlier admission. Remove the end-only kernel validation authority gap; no fabricated empty/default projection on missing history. |
| R2-2 Identity and assignment mapping | At applicable admission and commitment, bind registered actor definition/config/role/model/session/generation, full owner/branch, workspace/fence, workflow revision and immutable assignment to the actual kernel task/plan/grant identity. Reuse accepted validators and `projectKernelAssignment`; do not compare unrelated workflow and implementation revision counters, or supervisor identity with implementer fences. Define/review an explicit per-event mapping table rather than a blanket equality that makes valid flows impossible. |
| R2-3 Actual producer records | Retain closed concrete producer activation/intent/command/profile/nonce/input/control/grant/identity records and independent dispatch/settlement observations needed by correlation. An asserted intent ID/hash or caller witness alone is not proof of a historical producer. Retain settlement observation, phase and time. Use existing accepted types/validators where applicable; keep host provenance outside this pure model. Preserve a reachable initial supervisor-plan bootstrap rather than a plan/activation circular prerequisite. |
| R2-4 Supervisor candidate and commitment | Supervisor plan/answer/review/decision application requires the named actual producer's matching settlement already in the prefix. For kernel supervisor `decide`, bind the exact witness/control/operation/actor/activation/revision/input/retained receipt chain, then recheck applicable current eligibility, ownership/holds and correlation on its `intent-committed`. Do not use whichever activation/receipt happens to match a report. Keep supervisor-operation and kernel-operation namespaces distinct. Earlier receipt retention must not require future producer settlement; any remaining M4 content/retention limitation must be explicit. |
| R2-5 Kernel-derived actor facts | Correlate actor attempts/reports/effects/decisions to the actual retained kernel events/projection/commitment available at that prefix. Derive application/status from kernel facts where it is implementation-owned; do not introduce a second implementation reducer. Preserve separate Main/lifecycle cancellation, final-review approval and grant/continuation rules. Legitimate reuse of an operation across its own prepare/commit stages is a link, not an unrelated duplicate operation. |
| R2-6 Atomic outcomes | Route validation and reducer appends through the same correlation path. Preserve kernel `hold` and its retained model/journal instead of reporting `apply` or discarding required facts. Exact noop/conflict behavior remains atomic; actor-side holds must not fabricate an applied transition or silently erase observations. Invalid input never partially publishes mutated state; programming exceptions still escape class-specific expected-error handling. |
| R2-7 Preserve foundation | Keep capture before access, complete reconstruction/cache comparisons, strict scalar/closed constructors, deep immutable results and concrete deep-readonly types. Preserve exactly the five exports/arities/selected returns (below), no consumer, no extra/default export or registration. No any/suppression/unchecked cast used to hide a missing state or correlation. |
| R2-8 Honest scope and cost | Full remaining M3 lifecycle/obligations/reservations, M4 current-segment inspection artifacts, M5 budgets/accounting, M6 mailbox fairness/reserves, M7 archive carryover/quiescence/legacy and M8 aggregate work accounting remain separate. Do not weaken them to pass this stage or claim closure. Document the cost of per-prefix public kernel revalidation; never bypass checks, invent trusted contexts/results, start hidden budgets or claim aggregate-budget proof. If sound composition needs a private seam, report it precisely. |

The exact exports remain:

- `validateActorStateV2(value, context)` → `ActorStateValidation`
- `projectLegacyWorkerView(value, workerId, context)` → `LegacyWorkerView`
- `validateActorWorkflowModel(value, context)` → `ActorWorkflowModel`
- `validateActorWorkflowEvent(value)` → `ActorWorkflowEventV2`
- `reduceActorWorkflow(model, event, context)` → `ActorTransitionResult`

`context` is required untrusted `ArchiveValidationContext` data, not the registered budget capability. Initial roots still require the accepted explicit empty context. All results remain non-authorizing. Original V1 property-order hashes must not be canonicalized/replaced.

## Delivery and Main verification

Original R2 delivered partial `26eaffa0…`; C1 corrected focused F1–F3, and C2 corrected F4's historical-publication witness. Main fixed one missing private JSDoc brace and verified final `b5288b20…`, with 0 owned diagnostics and all frozen boundaries preserved. [Latest checkpoint](AR3-T08-R2-FINDINGS.md). The bounded correction set is verified; no full T08/R2 qualification, M3–M8 closure or external ABI change is claimed. All writers are terminal; live status remains in the task ledger.

The writer must implement, not merely return another plan. Give a nonempty final handoff: edited path; R2-1–R2-8 done/partial/blocked with exact symbols/lines; closed new/changed model-local shapes; per-kernel-kind correlation/issuer/commit table; source walkthrough of reachable initial-plan → dispatch → report → supervisor candidate → commitment, including future-fact rejection and held/noop/conflicting outcomes; exact missing ABI if any; remaining semantic gaps/costs. If blocked, preserve useful coherent edits and name the precise blocker. No blanket “M2/M3 complete” if a path is still bypassable or merely rejected wholesale.

Child checks were **NOT RUN, Main-owned**. Planned Main checks were stable source review, syntax/whitespace, exact AST signatures/import graph, frozen/protected hashes and unchanged full-config supplemental TS6.0.3 attribution. Final `b5288b20…` retains 24 roots/350 unchanged source diagnostics/0 actor-model/0 dependency-global, baseline SHA `7aab6bdc4b933b27bd72034ca5e8a6a7e4f57223a5cd757742c4c60e519a8730`, protected 19-file SHA `fd43f0f2dd50745aed4df6d007522e180ac6badd514da7085f2d1b369ef69738`. The final checkpoint additionally records one Main in-memory probe of actual isolated `runFold` with supplied kernel/hash hooks (11/11 expected outcomes), beyond the original static-only checklist. No tests/fixtures/runners were added or project ESM/native runtime loaded. This narrow evidence does not qualify the full model, broaden child permissions or waive the failing project build/runtime gates.

Main records actual closure/remaining findings before any later assignment; this is neither T09 nor a full T08 acceptance gate.
