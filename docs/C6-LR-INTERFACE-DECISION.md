# C6-LR — Main's bounded interface decision

## Authority / ownership

User's complete-all instruction permits necessary draft actor-model changes under explicit ownership. C6-KA is checked; C6-PREFIX31b02061… has24 bounded outcomes and independent source-slice CLEAR. This opens **src/actor-model.js only** for the next returned writer ID. Common/record/wire/config/migration/facade/coordination/transitions, runtime and other files remain held. Five public signatures,27 kernel kinds, actual private prefix/fork, shared context and all numeric limits remain. No active format rollout; amended aggregate payload/view details must be re-reviewed and captured at T09/T10.

No child execution, tests/fixtures/runners/generated scripts, project ESM imports, native/provider probes, installations, settings, delegation, commits or outside-file edits. Main owns documents and bounded checks. Stop with an exact ABI blocker instead of guessing an outside-file change.

## LR1 — terminal logical disposition, not erased facts

Current workflow-closed checks physical activations/grants/effects/notices, but workflowObligations includes retained held commitments/reports/questions/review intents that do not gate disposal or unresolvedCount. The reviewed pause→held storage approval→notice resolution→escalate/dispose sequence can release a slot while those logical debts remain.

Main approves an **optional closed disposition payload on workflow-closed**, using its existing observationId as the disposition identity, with a current-Main-bound HumanAuthorization and exact named logical obligation IDs. The implementation may define precise private typedefs/view records but must preserve absent optional fields when parsing historical events—no invented default payload fields that change old root/producer hashes.

- Physical reservation/effect/transport/notice checks remain non-waivable. First establish physical drain; logical disposition cannot forge a kernel barrier, ACK, notice resolution or native settlement.
- At a terminal outcome, require the unresolved logical set to be empty or exactly covered by an explicit authorized terminal abandonment/disposition. Reject unrelated, omitted, extra, duplicate, conflicting or stale-owner authorization references. A later new debt is not covered by an old blanket marker.
- Preserve all original held commitments, reasons, reports/questions/control inputs and kernel results. Append immutable disposition evidence and derive which logical refs remain unresolved; do not delete facts, retroactively approve denied consequences, refund usage, reopen terminal state or rewrite kernel-derived outcome. A disposed workflow with newly uncovered logical/physical debt continues to count as unresolved.
- Existing closure without disposition remains supported only when logical debt is actually empty. A historical closure that relied on the reviewed safety hole must remain retained/held where representable, not silently retain its erroneous slot release. Do not fabricate a human disposition to make old data look clean.
- This is an abandonment/attention disposition, not report adoption or new authority. Accounting uncertainty and unrelated holds do not become resolved merely because a workflow is terminal. Preserve ordinary/reserved capacity policy; mailbox-wide control reserves remain separate M6 work.

## LR2 — evidence-named supervisor unknown resolution

Keep original usage records immutable. Main approves additive **optional resolutions on supervisor-accounting-reconciled**, scoped to the original producer, activation, operation, workflow and metric. A resolution names an actual retained null-valued observation plus an actual retained known observation of the same scope/metric as its replacement evidence. No new evidence artifact kind or kernel change is authorized.

- Require current Main as the trusted-accounting observer and exact original binding. Source tags alone still authenticate nothing; this remains a non-authorizing model.
- Named original/evidence records must exist at the reconciliation prefix/time, match complete scope, and use a one-to-one replacement relation per metric. No resolution of another activation/producer, known-value overwrite, arbitrary price/zero, evidence reuse for multiple unknown charges, cycles, fabricated IDs or unmatched coverage.
- Preserve raw nulls, known evidence and immutable resolution provenance. Count the replacement evidence only once, not once as its own observation and again through the resolved null. Other genuine observations still add; do not blanket-deduplicate by activation/metric.
- Completeness still covers the exact full original observation set and all three metrics. Each entry must be known or have a valid named resolution. Additional observations invalidate completeness until covered again. Keep prior resolution/proof history when completeness becomes stale; do not discard valid provenance via a.accounting=null.
- If retirement/delivery/native facts invalidate prior completeness, it becomes unknown again without deleting known lower bounds or resolutions. No abort/transport-close alone can certify a completed producing run. Use actual native completion or appropriately complete containment, with original temporal/scope checks.
- Preserve absent old optional fields and all old canonical hash domains. An ABI need beyond this actor-only evidence mapping is a blocker, not permission to change common or kernel.

## LR3 — independent settlement and original output facts

Main approves private/public derived settlement history per activation, retaining aborted, transport-closed and native-settled independently. A native fact must not be rejected solely because a different phase was already observed.

- Preserve original observation IDs/times and producing tuple. Exact retry is inert; changed original or incompatible same-phase facts conflict rather than overwrite. Derive native completion separately. Delivery/ACK, native execution, effect settlement and transport/containment remain distinct.
- Update every consumer: historicalSettledIntent/reviewer linkage, reservation/readiness, containment coverage, accounting completeness and views. Legacy singular settlement fields may remain as descriptive compatibility projections but cannot make abort/transport closure count as native completion.
- Add optional complete settlement-observation coverage to activation-contained as needed in this actor payload. Old singular coverage only covers its actual sole fact; it cannot silently cover later independent facts. Later uncovered delivery/settlement invalidates containment/reservation release. No reopening a grant or erasing a late fact to preserve a slot invariant.
- Add optional closed original output observation `{observationId, at}` to supervisor-intent-recorded. Preserve omission in old canonical payloads. Retain both production evidence and receipt time in the derived intent; do not fabricate an original native observation when absent. Historical receipt time is only a conservative upper bound, not a new claimed production timestamp.
- Bind original output to its existing producer/control/activation, actual start/report/checkpoint and immutable request identity. Use production time for native-completion correlation, not receipt time. Retain structurally valid bound late output as observation/held evidence rather than discarding it merely because receipt follows settlement or current expiry/terminal state.
- Retention must not grant consequence authority: current owner/lifecycle, ACK/discrepancy, native retirement, immutable deadlines/receiver high-water, plan/report/checkpoint/evidence and all existing actor/kernel gates still apply. No backdating can revive expired permission; no late plan rewrites a dispatched task (structural replanning remains C6-PLAN). Preserve prior held commitment denial even after later holds resolve.
- Distinguish missing intrinsic producer/evidence correlation from current admission denial; do not replace all failures with success or all valid workflows with permanent holds. If a genuinely missing correlation needs another contract, stop with the exact seam.

## Main correction / namespace ratification after independent LR review

Reviewer b3efd56de9034bd1893a04a4eda6b489 established two defects on stopped cd84cda3: operation-level suppression loses a later denied publication's provenance, and stored transient observational admission denial prevents recovery after pause/resume. Main opens actor-model.js only to retain distinct denied storage observations and reevaluate current output-consumption eligibility. Keep original held commitment denial permanent and all intrinsic/current gates; do not convert a receipt into a commitment. See C6-LR-FINDINGS.md. All other source remains held.

**Observation identity decision:** for the new actor output/settlement fields, observationId denotes the durable identity assigned by the trusted Host mapping in AR3-01-DECISIONS.md’s producer-observation ingress decision, not a raw provider message ID or generation-local ordinal. Within an actor store, that durable ID belongs to one original producing activation/tuple; it cannot be reassigned to a different actor/session/generation/activation, including after rotation. The producing tuple remains mandatory—an ID alone is not proof. Exact actor/kernel projections may refer to the same original native fact for the same producer.

The Host must durably map the actual ingress-stamped tuple (retained dispatch binding, local source/sequence and original activation token) before queueing and preserve mapping across retries/rotation. Equal local ordinals from different streams map to different durable identities. Replay never invents or renames missing/colliding IDs, and a label/hash authenticates nothing. Missing/ambiguous mapping requires retained diagnostic/unsupported handling at the Host boundary, not a fabricated original identity. That actual Host mapping remains an AR-04/runtime gate, not something this pure model implements.

This ratifies the existing cross-producer output/settlement rejection rather than widening it. It does not rewrite the explicitly producer-scoped usage union or other journal/reference ID namespaces. Historical originals and bytes must be preserved; an ambiguous historical collision must not be silently repaired into purportedly valid authority. T09/T10 must include this namespace and Host obligation in the field/reader/writer/crash matrix.

## Required handoff / acceptance boundary

Trace the reviewed disposal sequence before/after disposition; null→named-known recovery without doublecharge and with fresh later unknowns; abort/transport close→native settlement; containment followed by uncovered fact; output produced before retirement but received later; expiry/terminal/stale-owner observations without consequence revival; exact retry/conflicting identity; malformed references; source/publication/work exhaustion and immutable prefix failure. All new scans/forks/derived representations remain metered in the existing context.

Child reports source walkthroughs, changed typed fields/parser/reader/writer paths, remaining blockers and all execution NOT RUN. Main performs stopped checks and independent review. This does not close full initial-root→approval→mirror, source preimages, quantitative two-chain resource fit, M6/M7, T09/T10 or runtime qualification.
