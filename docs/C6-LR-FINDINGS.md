# C6-LR — stopped candidate review and correction

## Candidate / checks

Writer `d70eebcf5b3d445bb0724bb52bd2402f` STOPPED, actor-model.js only. Main audited the actual native log: package.json first,32 read/grep/edit calls,6 edits to the sole assigned file. Candidate SHA `cd84cda3daa0c835312bc55e356f089d2c39e15b154345f92e39e74a21e46991` passes syntax/whitespace and the same all24-root actual SDK/TS6 supplemental check with zero diagnostics. Five aggregate arities2/3/2/1/3 and eight protected core hashes remain. Not pinned/runtime certification.

Main executed24 bounded outcomes:9 actual public empty-root→workflow submission→human escalation→debt-free closure/disposition→new submission/replay cases, with real normalized config and budget/config digests;15 explicitly private helper cases using controlled debt/activation/usage inputs over the real submitted workflow and empty kernel. The latter cover provenance-qualified logical debt, native vs abort, complete containment/new uncovered facts, null resolution once, same-ID new metric/accounting invalidation and original production vs receipt time. They are NOT public activation issuance/reconciliation/storage-publication histories, complete producer validation, full workflow or quantitative fit. No tests/fixtures/runners, project ESM or native/provider execution.

The public submission inputHash remains under the existing draft schema; these checks do not repair or certify the separate original-submission-preimage gap.

## Independent review — BLOCKED

Read-only reviewer `b3efd56de9034bd1893a04a4eda6b489` STOPPED, no execution/edits/hash recomputation. Two concrete defects:

1. **P1, new denied publication hidden by operation-level retention.** applyImplementation retained a held fact only if its operation had no previous held commitment. Dispose the denied grant commit G/C, then retain a late denied closed-grant authority publication G/P: the later fact was not added to logical provenance. Once physical debt drained, the earlier disposition could implicitly waive G/P. Witnesses on candidate:1960–1966,1414–1440,2455–2480 and kernel1054–1070.
2. **P2, observational pause becomes permanent consequence denial.** A valid plan produced before pause but received while paused was retained with producer-ineligible. After native completion/accounting/resume within deadlines, plan-recorded still failed because intentAdmissionDenial preferred that old diagnostic. No authoritative commitment had been attempted. Witnesses1374–1376,1609–1651,2048–2066,2162–2173,2231–2259. Main's component observation also showed current producer denial null while stored intent denial remained producer-ineligible; the reviewer established the supported source counterpath.

No other concrete LR defect was established in optional omission, named metric replacements, histories, independent coverage or private-prefix publication. This is not behavioral clearance. The reviewer could not establish global native observation namespace from the older contract; Main explicitly ratifies it in the [interface decision](C6-LR-INTERFACE-DECISION.md).

## Main correction — stopped checks passed, independent re-review pending

Only actor-model.js is opened for these stopped-review corrections:

- New private metered retainHeldCommitment retains every distinct denied storage observation, preserving operation/phase provenance and initial receipt/reason on retry. Operation-level heldCommitment still permanently denies authority. Logical disposition matches each retained observation, not an operation blanket.
- Receipt-time admissionDenial remains visible but is not an authority decision. Consuming paths reevaluate current producer/lifecycle/holds/expiry, while actual historical output/native correlation, owner/binding/retirement, dispatch rules and prior held-commitment denial remain.
- No namespace code widening, ID synthesis, kernel/wire/limits/API-signature changes. Host-mapped durable output/settlement identity ownership is now explicitly scoped in Main's decision; raw local sequence is not that identity.

The preceding24 checks describe cd84cda3. Corrected actor-model SHA `2590f1f1623b1ebd9cce92a866adfc21238bfaee81d0cb834fd8b46e3d6ab2f1` passes syntax/whitespace and the same all24-root supplemental compiler with zero diagnostics. Main mechanically confirmed one actual applyImplementation retainer callsite, five unchanged export arities and eight protected core hashes. **17 targeted correction outcomes** pass: distinct storage phases/fresh logical provenance, exact retry preserving original receipt/reason, phase/operation conflict atomicity, immutable base, current pause plus postresume consumption, retained diagnostic, owner/held-commitment/retirement/producer+control expiry/terminal gates, omitted original production evidence rejected by historical correlation, and real remaining-work exhaustion before mutation. Work80284 before the actual shared drain; no reset. These use actual corrected helpers/consumers over a genuinely public submitted workflow/registered-context fork with explicitly controlled private producer/debt inputs—not a full public storage/publication or pause/output journal. Corrective reviewer `c8c156cc4a5b48bf82354ba94e87febe` STOPPED **BLOCKED**: the two original fixes are source-sound, but the newly ratified namespace still admitted cross-family collisions. Output uniqueness checked only outputs; settlement/native-kernel checks only settlements. A's output X could therefore be reused as B's settlement X, and vice versa (2590f1f lines2269/2296/1777). Main added a shared metered checkProducedObservationOwner at all three ingress/correlation paths. It examines retained outputs and actual actor/kernel settlement facts, rejects a different producing activation, preserves existing exact per-family correlation and same-producer projections, and leaves usage identity unchanged. The namespace correction is now source329bdcf0295e3ba7fc98518ed497369d436b5d34f6bd53c03abea2523553e260. Ten focused component outcomes pass (both cross-family orders, same producer, unrelated ID, kernel native data before actor mirror, cross-workflow ownership, no mutation and real work exhaustion; work1750 before drain). These use explicitly controlled private collection/kernel-observation inputs, not validated whole native histories. Main confirms the three exact guard caller branches, five signatures/eight protected hashes, syntax/whitespace and all24-root supplemental zero diagnostics. Read-only namespace reviewer `1a04be95e37d43ee858d80493c01d9f1` STOPPED **CLEAR for corrected LR source slice only**. It independently traced both arrival orders, all three hooks, pre-mirror native facts, same-producer correlation, no mutation/shared charging and preservation of the two preceding fixes. No further concrete defect or source correction requested. It ran no checks or hash recomputation. This accepts the bounded LR source correction, not Task8, T09, full workflow/resource fit or runtime qualification. Source acceptance, full path, M6/M7, T09/T10 and runtime stages remain open. See [live ledger](COMPLETION-EXECUTION-PLAN.md) before assigning any next writer.
