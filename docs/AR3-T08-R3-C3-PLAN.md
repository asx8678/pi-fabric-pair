# T08-R3-C3 — Public-path reachability and local operation accounting

**Historical C3 execution:** see [completion execution](COMPLETION-EXECUTION-PLAN.md) for current C4/DUR-01 ownership. At the C3 checkpoint: Astra `33942617876d450fa8de108294d83f61` completed and stopped. Main verified bounded `a41af8da…`: 23 isolated local-boundary outcomes, zero new diagnostics and frozen/protected scope. [Findings](AR3-T08-R3-C3-FINDINGS.md) keep full public-path/work qualification incomplete. C3 is terminal; no agents running. **7/10 accepted**, original criteria unchanged.

## Authority, baseline and ownership

The user said **“do next”** after Main identified C2-7/8's public reducer → supervisor approval → mirror gap. This authorizes this bounded continuation, not reopening completed runs or accepting T08. **7/10 accepted; T08 partial/BLOCKED, T09/T10 unstarted.** Live ownership belongs to [the task ledger](AR-03-AGENT-TASKS.md).

Main reconfirmed `/Users/adam2/projects/pair/pi-fabric-pair`, model SHA **`3b20e31337c1aa441c499a34bb16b8d71b1c503f1b9de88fceb60e8f2b443aaf`**, all eight frozen hashes and protected 19-file SHA `fd43f0f2dd50745aed4df6d007522e180ac6badd514da7085f2d1b369ef69738`. No live agents at assignment preparation.

- One approved Astra `openai-codex/gpt-6-astra`, high, nonrecursive, **read/grep/find/ls/edit only**, extensions enabled. Mandatory first tool action: absolute repository `package.json` read, verify `pi-fabric-pair@0.1.0`, stop with zero edits on mismatch. No shell identity command.
- Sole production edit authority: **existing `src/actor-model.js`**, focused edits. Main owns docs/shared/integration/checks. No whole-file rewrite, extra public exports, independent trusted registry or active consumer.
- Six accepted contracts, `coordination.js`, `transitions.js`, Controller/runtime/evidence, configs/defaults/formats/registrations and all other production files stay frozen. A genuinely required external seam must be reported with exact symbol/arguments/result/caller/validation/accounting semantics before any outside edit.
- No concurrent writer/reviewer, recursion, bash/write, execution workaround, tests/probes/fixtures/runners/generated scripts, project ESM loads, installation or native/runtime activation. Do not read/use/modify `/tmp/tscheck.mjs`. Preserve unrelated dirty work including `%1`. Child commands/checks **NOT RUN — Main-owned**.
- Read [testing policy](TESTING.md), [C2 evidence and limits](AR3-T08-R3-C2-FINDINGS.md), [C2 criteria](AR3-T08-R3-C2-PLAN.md), [R3 criteria](AR3-T08-R3-PLAN.md), [R2 evidence](AR3-T08-R2-FINDINGS.md), [D1–D6](AR3-01-DECISIONS.md), [T01 ABI](AR3-T01-INTERFACE.md), [T06](AR3-T06-HANDOFF.md), [T07](AR3-T07-HANDOFF.md) and relevant actual source. Prior runs remain terminal.

## Main's witnessed execution path

At the starting hash:

1. `reduceActorWorkflow` (2089–2132) captures/parses the proposed event, reconstructs the untrusted root **or complete returned view**, compares every supplied cache, checks duplicate/sequence/reserve, then composes a new candidate in the same registered context.
2. **Concrete local accounting gap:** its ordinary reserve-size check (2108–2113) calls `encodePairJSON(candidateState, createValidationContext())`, explicitly bypassing the operation context. This local gap is now in scope; do not continue claiming whole-operation accounting while preserving that reset. Sharing the existing context must retain the same reserve meaning and count newly constructed work honestly.
3. `composeModelInContext` (1996+) captures/parses the root, validates/binds archive context, calls `runFold`, derives usage/views and returns the full model. Review whether each newly materialized result is bounded/charged under the existing common provenance rules. No synthetic equality credits or skipped copies.
4. `runFold` (1064+) starts from actual genesis. Every activation checks its exact earlier root hash; implementers additionally need a witnessed applied/open kernel publication. A caller-built projection or mocked proof lookup is not a reachable-prefix demonstration.
5. Planning needs actual supervisor dispatch, started observation, bound output and settlement before `plan-recorded`. Kernel dispatch then binds the settled plan, goal/steps/assignment/deadline. C2's base is recorded after committed dispatch but before any grant preparation. Implementer activation follows actual grant preparation/commit/publication and precedes its prompt preparation/commit/delivery/report/effect/checkpoint facts.
6. Review uses a separate supervisor goal activation, started observation and exact review output. Its immutable request/reply/proof/delivery/receipt chain precedes kernel receipt. Native reviewer settlement and operation-bound current-checkpoint evidence must satisfy the frozen kernel before the distinct decision candidate/commit. Actor report/inspection/decision mirrors have their own actual prior-record prerequisites (1694–1740); do not omit them in a claimed positive path.
7. Current admission and retained late observations remain separate (`correlateKernel`, `kernelAdmissionDenial`, `applyImplementation`). A held commitment is visible but never promoted by its mirror or by C2 base advancement.
8. Empty context must really be empty (`bindArchive`, 1864+). Prior-checkpoint carryover remains unsupported. Frozen public kernel calls still revalidate growing journals; source at 1238+ explicitly documents this repeated work. Closing the local reserve leak or demonstrating a small path is **not full M8 closure**.

C2's 44 isolated outcomes and 8/16 reference measurements used actual selected functions/common helpers but supplied kernel and producer facts. Do not repeat those as if they exercised the public aggregate.

## Acceptance ledger

| ID | Required result |
|---|---|
| C3-1 Concrete initial-root witness | Give a **human-readable source obligation table**, not a runnable fixture/script, for one ordinary supported workflow: two configured actors, one step, one real `a.txt` change `old\\n` → `new\\n`, complete structured proof and supervisor approve. Trace each actor/kernel event from the truly empty initial context, including all required native delivery/ACK/settlement/effect/accounting facts. Every consumed record must be established by an earlier actual branch, never inserted into a derived map. |
| C3-2 Exact vertical bindings | For each phase identify outer event/time/sequence, actor/generation/session/owner, task/step/attempt/lease/fence, activation/command/intent/operation namespaces, digest preimage and exact earlier source. Include original V1 order, authority/root-prefix proof, dispatch and review control hashes, report finalization, inspection logical IDs versus artifact refs, all-scope/current-checkpoint approval, distinct kernel decision operation, commitment and required mirrors. A nine-stage prose sketch is insufficient; name every prerequisite/predicate that could make the path unreachable. |
| C3-3 Shared local public budget | Remove the witnessed fresh-context reserve-size bypass using the existing operation context. Preserve ordinary/reserved capacity semantics, no-op behavior, capture-before-access and no partial authorizing outcomes. Charge newly constructed data honestly; avoid redundant reconstruction where genuine same-operation provenance permits reuse. Report known capacity failures as capacity rather than disguising them as unrelated input conflicts where the existing result type permits it. No cap increases, budget reset or equality credit for separate sources. |
| C3-4 Returned-view boundary | Trace both canonical-root input and the actual full returned `ActorModelView` through `reconstructModelInContext` and candidate composition. Preserve complete cache comparison, required five signatures and deeply immutable non-authorizing output. Verify/repair bounded materialization of returned views in this file where necessary; no caller-visible trusted cache marker or shortcut around historical reconstruction. |
| C3-5 Reachability and work attribution | Account for the **entire selected public operation**, not only source retention: incoming event, root/view capture, parse/leaf validation, archive binding, source/reference occurrences, historical root hashing, each growing kernel replay, reserve sizing, candidate replay and final views. Distinguish actual shared-context charges, separately bounded frozen helpers and unmetered/deferred work. Give measured evidence only if Main ran it; otherwise give a defensible source bound with explicit assumptions. Do not manufacture a total, infer full M8 from 16 references or claim a frozen-helper cost was charged merely because its output was encoded. |
| C3-6 Witnessed corrections only | Fix concrete in-file path blockers and the explicit C3-3 local gap; do not rewrite working C2 content predicates or add speculative lifecycle policy. If no additional blocker is found, say so and retain source. If an outside seam or a genuinely broader M8 dependency prevents a requirement, stop at that boundary and report its exact mechanism/contract. Denying every approval is not a positive result; simulated kernel results are not proof. |
| C3-7 Failure preservation | Source-trace missing/future/changed evidence, wrong bindings/bytes/scope, absent or premature settlement, stale ownership/generation, duplicate/conflict and capacity boundaries. Preserve intrinsic-conflict rejection, no-append proposal hold, truthful late committed observations, exact held actor consequences, no base advancement on held/noop and no future repair. Maintain Main/lifecycle containment branches separately from supervisor approval. |
| C3-8 Honest delivery | Return exact edited path and C3-1–C3-8 plus inherited C2-7/8 and R3 rows as done/partial/blocked with symbols/lines. Include the complete source obligation table, local accounting changes, remaining independently bounded/unmetered work and any exact ABI dependency. No tests/checks run by child. No complete T08/runtime/Host or T09 claim. |

A source-only witness must be labelled **unexecuted**; it may support manual review but is not a measured public-run result. Merely removing a known local reset does not establish all original public-path criteria. No requirement is weakened or marked complete because the no-tests policy limits evidence.

## Main's post-delivery checks

Only after the writer stops: inspect exact edits and witness, verify syntax/whitespace, frozen/protected hashes, five exports/arity/required parameters, new internal registration changes if any and dependency graph. Use the existing full-config supplemental compiler attribution; baseline is TS6.0.3, 24 roots, 350 unchanged other-source / 0 owned / 0 dependency-global diagnostics, exit 2, diagnostic SHA `7aab6bdc4b933b27bd72034ca5e8a6a7e4f57223a5cd757742c4c60e519a8730`.

Main may perform bounded one-off in-memory checks of changed actual functions, with real common helpers and explicit supplied-data boundaries. No test files/runners/project ESM/native loads. Do not relabel another mocked/internal chain as public execution. Record partial/blocked evidence honestly and preserve all broader M3/M5–M8, archive, Host and runtime gates.

Public exports remain exactly `validateActorStateV2`/2, `projectLegacyWorkerView`/3, `validateActorWorkflowModel`/2, `validateActorWorkflowEvent`/1, `reduceActorWorkflow`/3, all parameters required. Frozen signatures/formats and explicit empty initial archive context remain unchanged. No next stage is auto-authorized by a completed run.
