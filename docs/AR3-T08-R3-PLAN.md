# T08-R3 — Current-segment inspection and the vertical decision path

**Historical R3/C1/C2/C3 checkpoint:** current follow-on ownership is in [completion execution](COMPLETION-EXECUTION-PLAN.md); those original runs remain terminal. Main verified [bounded C3 `a41af8da…`](AR3-T08-R3-C3-FINDINGS.md): shared local accounting, final-view bounds, capacity-code preservation, 23 isolated outcomes and 0 new diagnostics. Full public-path/fit remains unverified; leaf/kernel M8 needs separate scope/ABI approval. Original eight-row R3 ledger unchanged and incomplete. **7/10 accepted; no full R3/T08 acceptance.**

## Authority and boundary

The user said **“do next”** after Main identified current-segment inspection and full vertical-path correlation as the next bounded work. This authorizes a new sequential implementation stage, not a restart of R1C/R2/C1/C2 or a change of model owner. **T01–07 remain accepted (7/10); full T08 remains BLOCKED; T09/T10 are unstarted.** Live ownership belongs in [the task ledger](AR-03-AGENT-TASKS.md).

- Main reconfirmed canonical repository `/Users/adam2/projects/pair/pi-fabric-pair` and starting `src/actor-model.js` SHA **`b5288b201ea080398866713b7987cca280760548f4242bac504fef050abed639`** before assignment. [R2 evidence](AR3-T08-R2-FINDINGS.md) is preserved, including F1–F4, observation/admission separation and historical publication witnesses.
- Same approved model: `openai-codex/gpt-6-astra`, high reasoning, one nonrecursive writer with **read/grep/find/ls/edit only**, extensions enabled. First child action: read absolute repository `package.json`; verify `pi-fabric-pair@0.1.0`, otherwise stop with zero edits. No shell identity command or alternate execution/provider workaround.
- Exclusive edit authority: **existing `src/actor-model.js`**, focused edits, no whole-file rewrite. Closed model-local retention/event/view types required below are allowed; enumerate every added field and event in the handoff. No arbitrary new public API, trust flag or independent stored registry.
- Six accepted contract modules, `coordination.js`, `transitions.js`, active formats/config/defaults/examples/registrations/guard and all other production files remain frozen. Main owns shared/common/facade/docs/integration decisions. Graph adjacency is not edit authority. An actual outside-file need must first be reported with exact symbol, arguments, return type, validation/accounting semantics and caller; stop at that boundary rather than edit it.
- No concurrent writer/reviewer, child delegation, bash/write, tests/probes/fixtures/runners/generated scripts, project ESM execution, dependency installation, live/native workers or runtime activation. Do not use/read/modify/delete `/tmp/tscheck.mjs`. Preserve unrelated dirty work, including `%1`. Main checks only after the writer stops.
- Read [testing policy](TESTING.md), [D1–D6](AR3-01-DECISIONS.md), [T01 shared ABI](AR3-T01-INTERFACE.md), [T06 exact inspection/archive types](AR3-T06-HANDOFF.md), [T07 kernel obligations](AR3-T07-HANDOFF.md), current R2 evidence and relevant source. Older generic writer/shell wording does not supersede this assignment.

## Witnessed path at the starting hash

1. `resolveInspection` (`actor-model.js:579–614`) resolves only a `Map<ref,string>` provided by archive binding, decodes the entire wrapper as if it were bare content, hashes that wrong layer and checks only part of the content. It does not supply exact accepted wrapper closure, declared request/reply byte-size equality, reply evidence qualification, finite request deadline or receipt delivery correlation.
2. T04's real wrappers are **`{content,digest}`**, with `digest = pairDigest('inspection', content, context)` (`actor-wire-contracts.js:64–78,293–330`). `inspection`, `payloadRef`, `inspectionRef`, `observation`, `scope` and `envelopeSize` are private source-of-truth rules, not available imports. Public control/mailbox wrappers create standalone contexts (`520–551`). Existing common primitives are available; no missing external ABI is established merely because a private helper exists.
3. `bindArchive` (`1361–1404`) keeps the initial context explicitly empty, then puts all chain artifact original texts into one resolution map before replaying archive segments. Byte resolution is not proof of prior retention. The initial workflow currently has no retained current-segment artifact path; widening the empty-context schema is forbidden.
4. `reviewIntent` (`776–784`) already binds the actual earlier supervisor output control and permits receipt retention before native settlement. `correlateKernel` (`878–897`) correlates supervisor receipt links and settled decision witnesses but does not qualify resolved inspection contents. A checked actor mirror alone cannot close this kernel route.
5. `applyImplementation` (`967–993`) handles noop before correlation, validates intrinsic historical relationships, applies current admission and preserves exactly bound late `intent-committed` observations as non-authorizing held consequences. It re-correlates the immutable original candidate on commitment. Preserve this distinction.
6. Actor `inspection-recorded` (`1194–1212`) requires an already retained exact kernel receipt and then calls `resolveInspection`; actor `decision-recorded` (`1214–1236`) mirrors an already committed kernel decision. Requiring the actor inspection mirror before kernel receipt retention would create a cycle. The artifact-retention path must be earlier and independent of that mirror.

Historical M4 prose in [T08 findings](AR3-T08-FINDINGS.md) predates R2: settlement-free `ReviewLink`, explicit scope and actual producer linkage have already been corrected. Do not redo those fixes or restore their older defective form.

## Acceptance ledger — R3 only

| ID | Required implementation and verification |
|---|---|
| R3-1 Retained current sources | Provide a bounded, closed, canonical model-local path to retain original inspection/evidence sources before first use. The first workflow with the unchanged explicit empty archive context must have a reachable inspection path. Reconstruct private indexes from journal prefixes, including archived-root replay; later events or global archive resolver contents must not repair earlier admission. Raw byte availability alone is never retention authority. Define exactly how any prior-checkpoint sources qualify; unsupported carryover remains explicit/fail-closed, not silently trusted. No extra archive-context field or independently writable cache. |
| R3-2 Exact T04 contents | Validate request/reply/receipt as the accepted closed `{content,digest}` wrappers; compare wrapper digest, reference content hash and inspection-domain hash of content, never the raw-byte hash or whole-wrapper hash. Match all accepted scalar, scope, reference, deadline, observation, payload encoding/domain and envelope rules. Bind `requestBytes`/`replyBytes` to exact retained UTF-8 source lengths, distinct logical IDs and distinct artifact values/content hashes. Ref text is a locator, not a logical ID and need not be path-unique: index/check full ref/hash identity, reject conflicting source replacement, never overwrite by locator alone. Preserve V1 original-order payload hashes and raw-decoder duplicate-key rules. |
| R3-3 Whole historical chain | Bind all three contents to the exact actual supervisor actor/model/session/generation/profile, workflow/revision, activation, original review intent/hash/operation, retained report payload hash, checkpoint and scope. Derive request/reply/receipt IDs from checked content and match kernel/actor declarations. Qualify every declared evidence reference with its encoding/domain/exact bytes and retained content as applicable; explicitly map actor evidence IDs to real retained report/evidence facts rather than treating ref strings as IDs or ignoring the field. Enforce finite request/reply/delivery ordering against recorded event times; explain the delivery-observation versus kernel-retention mapping rather than inventing equality or conflating delivery with native/effect settlement. |
| R3-4 No kernel bypass or cycle | Qualify supervisor `evidence-receipt` at its own earlier prefix, and supervisor `decide` candidate plus its immutable `intent-committed` through the same checked chain. An optional/later actor mirror cannot be the only gate. Retained links or hash-only references are not sufficient. No future settlement is required just to retain an earlier receipt; actual producing native settlement is required before supervisor decision application. Preserve separate supervisor/kernel operation namespaces, original Main receipt/decision and lifecycle cancellation paths, and all-scope/current-checkpoint approval and continuation predicates. |
| R3-5 Outcomes and late facts | Preserve exact noop/conflict atomicity, kernel hold and retained journal facts, class-specific input-error handling and programming-error escape. Reject malformed/cross-bound required observations, but retain intrinsically valid late commitments with R2's denied/held actor consequences when current eligibility changes. Do not reinterpret a timely earlier inspection as invalid merely because its request deadline is past at a later storage commitment; fresh admission and historical truth are distinct. No automatic reauthorization or clearing of held commitments/obligations. Any genuinely inseparable new lifecycle rule must be minimal, explicit and justified; general M3 reconciliation is not silently included. |
| R3-6 Concrete immutable reconstruction | Add closed discriminated concrete payload/index/view types only where necessary; no broad unknown map masquerading as checked content, `any`, suppression, unchecked state fabrication or trusted cast to evade validation. Capture before access, deep freeze results, expose deep-readonly public types and rebuild/compare every added derived field through both validation and reduction. Keep exactly the five existing runtime exports/signatures below, no production consumer or registration, and no mutable second registry. |
| R3-7 Honest bounded work | Apply existing envelope/root/reference/count/depth/node/byte bounds through the existing operation context. A locator or out-of-line payload is not a size-limit bypass. Reuse an explicit already-captured/decoded source only with genuine same-operation provenance; content equality of separately supplied copies earns no credit. No fresh hidden validation contexts, budget resets or synthetic trusted control/mailbox wrapper solely to call a leaf. Record remaining repeated kernel/prefix/hash and aggregate work costs; full M8 closure is not asserted. If exact reuse requires a new private seam, report it before any frozen-file edit. |
| R3-8 Vertical delivery and limits | Walk a reachable initial empty-context workflow through supervisor plan, committed grant/publication and implementer dispatch/report, supervisor review intent, prior artifact retention, kernel receipt, actor inspection, separate producing settlement, supervisor decision candidate/commit and actor mirror. Show the actual code ordering and fields, plus future-source, mismatched content/bytes/scope/delivery/producer, missing settlement, noop/conflict and late-held commitment paths. Retain M3/M5/M6/M7/M8 and all host provenance/runtime/qualification limits. No broad claim that Task 8 or the whole model is complete. |

### Validator reuse cautions

Prefer accepted exported common primitives for the exact model-local aggregate constructors. If a local inspection-content constructor is necessary, spell out parity with T04's accepted shape/scalar/hash/bound rules rather than loosely accepting arbitrary objects. Do not fabricate a control envelope or substitute a retained earlier intent for a different enclosing transport binding. In particular, T04's `intentHash !== binding.inputHash` check concerns an inspection message's own control input versus the **earlier** review intent, not inequality against that earlier intent's identical retained hash. Scope such checks to their actual available binding; report an actual missing dependency instead of constructing false authority.

`ArchiveResolvedArtifactV2.reference.hash` is an exact original-byte hash. `ArtifactRef.hash` for an inspection is the inspection-content digest. Keep both meanings distinct. Model-local retention sources must be bounded as actual canonical data; do not silently introduce external storage I/O or unbounded blobs. Neither a matching digest nor successful pure replay establishes physical inspection, host provenance or effect permission.

## Frozen public signatures

- `validateActorStateV2(value, context)` → `ActorStateValidation` (arity 2)
- `projectLegacyWorkerView(value, workerId, context)` → `LegacyWorkerView` (arity 3)
- `validateActorWorkflowModel(value, context)` → `ActorWorkflowModel` (arity 2)
- `validateActorWorkflowEvent(value)` → `ActorWorkflowEventV2` (arity 1)
- `reduceActorWorkflow(model, event, context)` → `ActorTransitionResult` (arity 3)

All parameters remain required; context is untrusted `ArchiveValidationContext`, not a budget capability. Initial contexts remain explicitly empty. Results remain `nonAuthorizing:true`.

## Delivery and Main checks

Implement the focused patch; do not return merely another plan. Give a nonempty terminal handoff with:

1. Exact edited path and R3-1–R3-8 done/partial/blocked, with symbols/lines and any precise missing ABI.
2. Every added/changed closed event/artifact/view shape, issuer and prefix availability rule, and the distinction between content digest, raw hash and exact byte count.
3. Per-use-site table for artifact retention, kernel receipt, actor inspection, decision candidate, late commitment and actor mirror; intrinsic observation versus current admission outcomes.
4. The source vertical walkthrough and counterpaths above (no child tests/fixtures), explicit costs and remaining lifecycle/archive/qualification gaps.
5. Child checks: **NOT RUN, Main-owned**. No passing compiler/probe/review claim on behalf of Main.

Main verifies the stopped source: source/structural review, syntax/whitespace, exact public symbols/arities/required arguments/return names/import graph, frozen/protected hashes and the same full-config supplemental TS6.0.3 attribution. Baseline: **24 roots / 350 unchanged source diagnostics / 0 actor-model / 0 dependency-global**, baseline SHA `7aab6bdc4b933b27bd72034ca5e8a6a7e4f57223a5cd757742c4c60e519a8730`, exit 2 (not a passing project build). Frozen six-contract/two-kernel hashes and protected 19-file SHA `fd43f0f2dd50745aed4df6d007522e180ac6badd514da7085f2d1b369ef69738` were reconfirmed before assignment.

No test files, fixtures or runners are to be added. After the source stops, Main may make bounded, one-off in-memory behavioral checks of the actual isolated changed functions without loading project/native modules or writing a test suite; report any supplied callbacks and unverified integration explicitly. Such checks do not qualify the full model/runtime, alter child permissions, close the project compiler gate or substitute for T09. No concurrent moving-source checks.

Main records verified closure or findings before any next assignment. This is a bounded M4/vertical-path stage, **not full T08 acceptance or T09**.
